box_size: 300

# Entity data definitions
entities_data:
  EntitySubTypes:
    - TADPOLES
    - DRAGONFLY_LARVAE
    - POND_STRUCTURE

  Entities:
    TADPOLES:
      type: AGENT
      num: 4
      existing: 4
      color: cyan
      diameter: 5.0
      prox_dist_max: 80  # Circular vision field
      prox_cos_min: -0.7  # 360-degree vision

    DRAGONFLY_LARVAE:
      type: AGENT
      num: 2
      existing: 2
      color: red
      diameter: 10.0
      prox_dist_max: 60  # Triangular vision field
      prox_cos_min: 0.90  # Narrow field of view

    POND_STRUCTURE:
      type: OBJECT
      num: 1
      color: green
